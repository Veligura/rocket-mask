import { NgxRocketMaskDirective } from './ngx-rocket-mask.directive';

describe('NgxRocketMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxRocketMaskDirective();
    expect(directive).toBeTruthy();
  });
});
